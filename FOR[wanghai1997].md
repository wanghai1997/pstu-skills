# 财报筛选器项目文档

> **写给 wang hai 1997**：这是一份关于"财报筛选器"项目的完整说明。我不会用枯燥的技术术语，而是像讲故事一样，让你明白这个系统是怎么工作的，以及我们为什么要这样设计。

---

## 一、项目是什么？一句话概括

**财报筛选器 = 一个能自动帮你"排雷"的财报分析机器人**

想象你要去超市买水果。货架上有几百种水果，你不能每个都尝一口。于是你会先看外观：有没有烂洞？颜色对不对？摸起来硬不硬？通过这些快速检查，你能排除大部分不好的水果，只留下几个值得买回家的。

财报筛选器就是干这个事的——只不过它看的是**上市公司的财务报表**。

---

## 二、为什么要做这个项目？

### 投资的困境

唐朝在《手把手教你读财报》里说了一句话，我印象特别深：

> **"财报不是用来找到好企业的，而是用来排除有问题的企业。"**

这句话什么意思？

想象一下：A股市场有几千只股票，你不可能每一家都深入研究。时间不够，精力也不够。如果像大海捞针一样乱看，很容易踩雷。

所以唐朝的策略是——**先快速排除，再深入研究**。

### 人工筛选的问题

但如果用人眼去看几千家公司的财报，会有什么麻烦？

1. **太慢了**——看一份财报要几小时，几千家要看几年
2. **太累**——看到第100份时眼睛都花了
3. **容易漏**——人脑会疲劳，有些异常信号可能没看到
4. **不标准**——今天心情好标准松，明天心情不好标准严

### 自动化的解决方案

于是就有了这个项目：**让程序帮我们做初步筛选**。

程序不会累，不会烦，标准永远一致。几秒钟就能扫描完一家公司的核心指标，告诉你"这家有问题，排除"还是"这家看起来还行，值得进一步研究"。

---

## 三、核心思想：像唐朝一样思考

这个系统的所有规则都来自唐朝的《手把手教你读财报》。让我用几个类比来解释这些规则：

### 规则1：ROE ≥ 15%

**类比**：想象你开了一家公司，投入了100万本钱。一年后，你赚了多少钱？

- 如果赚了25万，ROE就是25%——你是个经营天才
- 如果赚了5万，ROE就是5%——你还不如把钱存银行
- 如果亏了，ROE是负的——趁早关门吧

**为什么设15%这个门槛？**

因为巴菲特说过，长期来看，能持续保持ROE在15%以上的公司，就是优秀公司。低于这个数，说明公司的"赚钱能力"不够强，不值得深入研究。

### 规则2：毛利率 ≥ 20%

**类比**：你开了一家奶茶店，一杯奶茶卖20元，成本是15元。那你的毛利就是5元，毛利率是25%。

毛利率反映了什么？**你的产品有没有竞争力**。

- 毛利率 > 40%——你有独门配方，别人做不了，定价权在你手里（比如茅台）
- 毛利率 20%~40%——正常生意，能赚钱但也面临竞争
- 毛利率 < 20%——你做的东西谁都能做，只能靠低价竞争，很难长久

唐朝说毛利率低于20%的不投资，因为"没有护城河"的生意太脆弱。

### 规则3：应收账款占比 ≤ 30%

**类比**：你卖了一批货给客户，价值100万。客户说"我现在没钱，3个月后再付"。这100万就是"应收账款"。

问题来了：
- 这100万计入了你的"收入"，但你实际还没拿到钱
- 如果3个月后客户跑路了，这100万就打水漂了
- 如果客户一直拖着不付，你的现金流就断了

所以，如果一家公司的应收账款占总资产比例太高（>30%），说明它的销售回款能力有问题。利润表上看着赚钱，实际上都是"纸面富贵"。

### 规则4：现金流肖像

这是唐朝书里最精彩的部分之一。**三张现金流量表的正负组合，可以判断一家公司的"性格"**。

想象现金流是公司的"呼吸"：

| 经营现金流 | 投资现金流 | 筹资现金流 | 类型 | 含义 |
|-----------|-----------|-----------|------|------|
| +（赚钱） | -（扩张） | -（还钱） | **奶牛型** | 最理想的类型！经营赚钱，对外投资，还清债务 |
| +（赚钱） | +（收缩） | -（还钱） | 老母鸡型 | 成熟期企业，不再扩张，适合收股息 |
| +（赚钱） | -（扩张） | +（借钱） | 蛮牛型 | 正在大力扩张，要看项目前景 |
| +（赚钱） | +（收缩） | +（借钱） | 妖精型 | 有钱还借钱？可疑！ |
| -（亏钱） | +（卖资产） | +（借钱） | 骗吃骗喝型 | 经营亏钱，靠卖资产和借钱度日 |

**奶牛型 (+ - -) 最理想**——它像一头健康的奶牛，每天吃草产奶（经营赚钱），偶尔需要换个大一点的牛棚（投资支出），但不需要借钱（筹资流出，在还钱）。

---

## 四、技术架构设计

### 整体结构图

```
┌─────────────────────────────────────────────────────────┐
│                    财报筛选器                            │
│                (FinancialScreener)                      │
├─────────────────────────────────────────────────────────┤
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ 数据获取层   │  │ 指标计算层   │  │ 规则检查层   │  │
│  │DataFetcher   │→ │Indicators    │→ │Rules         │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
│         ↑                                    ↓          │
│  ┌──────────────┐                   ┌──────────────┐   │
│  │ 缓存管理层   │                   │ 报告生成层   │   │
│  │CacheManager  │                   │ReportGenerator│  │
│  └──────────────┘                   └──────────────┘   │
└─────────────────────────────────────────────────────────┘
```

### 为什么这样分层？

**类比：医院的检查流程**

想象你去医院做体检：

1. **数据获取层** = 抽血、拍片、做B超（收集原始数据）
2. **指标计算层** = 化验室分析血液、读片（从原始数据算出各项指标）
3. **规则检查层** = 医生对比正常值范围，判断有没有病（应用医学标准）
4. **报告生成层** = 打印体检报告（输出结果）

每一层只关心自己的事，层与层之间通过明确的接口传递数据。这样：
- **容易维护**——某一层出问题，只需修这一层
- **容易扩展**——想加新指标？只需改Indicators
- **容易测试**——每层可以独立测试

### 模块详解

#### 1. config.py —— "规则手册"

这个文件存放所有的**筛选阈值**。就像医生的"正常值参考表"：

```python
SCREENING_CONFIG = {
    "min_roe": 0.15,              # ROE最低15%
    "min_gross_margin": 0.20,     # 毛利率最低20%
    "max_receivables_ratio": 0.30, # 应收账款不超过30%
    # ...
}
```

**为什么单独放一个文件？**

因为投资策略会进化。今天你觉得ROE 15%够了，明天可能觉得要20%。如果阈值散落在代码各处，改起来很麻烦。集中配置，一目了然。

#### 2. data_fetcher.py —— "数据采购员"

这个模块负责从**AKShare**（我们的免费数据源）获取财务数据。

**技术细节**：
- 封装了AKShare的各种接口
- 自动处理上海（sh）和深圳（sz）交易所的代码前缀转换
- 支持重试机制（网络不稳定时自动重试）

**为什么叫Fetcher？**

因为它就像一个采购员，去AKShare这个"数据超市"买东西（财报数据），买回来交给后面的模块加工。

#### 3. cache_manager.py —— "仓库管理员"

**为什么要缓存？**

想象一下，你今天分析了茅台，明天想再分析一次。如果没有缓存，程序会再去AKShare下载一遍数据——慢、费流量、还可能被限制访问频率。

有了缓存，程序会先问"仓库管理员"："这批货（茅台的财报）咱们仓库有没有？"
- 如果有，而且没过期，直接拿仓库里的
- 如果没有，才去AKShare下载，然后存到仓库

**缓存策略**：
- 财务报表：缓存30天（季报不会天天变）
- 实时行情：缓存1天（股价天天变）

#### 4. indicators.py —— "化验室"

原始财报数据是"科目余额"——货币资金多少、存货多少、应收账款多少。

但我们要的不是这些原始数字，而是**有意义的指标**：
- ROE是多少？
- 毛利率高不高？
- 应收账款占比大不大？

Indicators模块就是干这个的——从原始数据计算出各项指标。

**技术细节**：
- 每个计算方法都有异常处理（数据缺失、除零等）
- 返回None表示无法计算，而不是崩溃

#### 5. rules.py —— "主治医生"

这是整个系统的**核心决策层**。

医生拿到化验结果，会对比"正常值范围"：
- 血压 120/80？正常。
- 血糖 15？太高，糖尿病预警！

Rules模块同理：
- ROE 25%？优秀！
- 毛利率 15%？太低，排除！
- 应收账款占比 35%？过高，预警！

**设计亮点**：`CheckResult` 类

每个检查项返回一个结构化的结果：
```python
{
    "name": "ROE",
    "passed": True,       # 是否通过
    "value": 0.25,        # 实际值
    "threshold": 0.15,    # 阈值
    "description": "ROE ≥ 15%"
}
```

这样后续生成报告时，可以很清晰地展示"为什么通过/为什么不通过"。

#### 6. screener.py —— "前台接待"

这是用户直接打交道的模块。用法很简单：

```python
screener = FinancialScreener()
result = screener.screen_single("600519")
```

它内部协调其他所有模块：
1. 调用DataFetcher获取数据
2. 调用Indicators计算指标
3. 调用Rules应用规则
4. 组装结果返回

**类比**：就像医院的前台。你说"我要体检"，前台帮你安排抽血、拍片、化验，最后把所有报告整理好了给你。

#### 7. report_generator.py —— "报告打印机"

生成两种格式的报告：
- **文本报告**：在终端直接看，快速预览
- **Excel报告**：保存到文件，方便后续分析

**Excel报告包含4个Sheet**：
1. **筛选汇总**：一眼看出哪些通过、哪些排除
2. **详细指标**：所有财务指标的数值
3. **检查明细**：每项检查的结果和说明
4. **风险提示**：具体的警告和排除原因

---

## 五、使用示例

### 场景1：分析一只股票

```python
from financial_screener import FinancialScreener

# 创建筛选器
screener = FinancialScreener()

# 分析贵州茅台
result = screener.screen_single("600519")

# 查看结果
print(f"股票: {result['name']}")
print(f"状态: {result['status']}")  # 通过/预警/排除
print(f"得分: {result['score']}/100")
print(f"现金流肖像: {result['indicators']['cash_flow_portrait']}")

# 查看警告
for warning in result['warnings']:
    print(f"警告: {warning}")
```

### 场景2：批量分析股票池

```python
# 你的观察列表
watch_list = ["600519", "000001", "000858", "002415", "600036"]

# 批量筛选
results = screener.screen_batch(watch_list)

# 筛选出通过的
passed = [r for r in results if r['status'] == '通过']
print(f"通过筛选: {len(passed)}/{len(watch_list)}")

# 生成Excel报告
from financial_screener.report_generator import generate_excel_report
generate_excel_report(results, "我的股票池分析.xlsx")
```

---

## 六、我们踩过的坑和学到的经验

### 坑1：数据源的字段名不统一

**问题**：AKShare不同接口返回的DataFrame，列名不一样。有的叫"净利润"，有的叫"归属于母公司股东的净利润"。

**解决**：在Indicators模块里，对每个字段都做了多重映射尝试：
```python
# 尝试多种可能的列名
net_profit = row.get('净利润') or row.get('归属于母公司股东的净利润')
```

**教训**：外部数据源不可控，一定要做好兼容性处理。

### 坑2：财务数据的异常值

**问题**：有些公司的财报数据是空值，或者为0。比如某家ST公司，净利润为0，计算ROE时会除零错误。

**解决**：所有计算都加上异常处理：
```python
if equity == 0:
    return None  # 无法计算，而不是崩溃
```

**教训**：财务数据质量参差不齐，程序要"容错"。

### 坑3：缓存文件的编码问题

**问题**：Windows下保存JSON缓存，中文字符有时会乱码。

**解决**：明确指定编码：
```python
with open(file, 'w', encoding='utf-8') as f:
    json.dump(data, f, ensure_ascii=False)
```

**教训**：Python3虽然默认UTF-8，但在Windows上写文件时还是显式指定编码最保险。

### 坑4：现金流肖像的判断

**问题**：刚开始时，我们认为经营现金流为正就是好的。但实际上，如果一家公司经营现金流为正，但投资现金流也为正（说明在卖资产），这可能是"混吃等死型"。

**解决**：严格按唐朝的八种类型分类，不能只凭单一指标判断。

**教训**：财务分析要多指标联动，不能孤立看某个数字。

---

## 七、潜在陷阱和避免方法

### 陷阱1：过度依赖历史数据

财报筛选器只能基于**历史财务数据**做出判断。但投资是面向未来的。

**例子**：一家公司过去5年ROE都是20%，但今年突然宣布主营业务要转型——历史数据就没用了。

**避免方法**：筛选器只是第一步，通过筛选的股票仍需人工深入研究行业前景、竞争格局、管理层能力等。

### 陷阱2：忽视会计操纵

财报可以被"化妆"。比如：
- 提前确认收入，虚增利润
- 把费用资本化，隐藏亏损
- 通过关联交易转移利润

**避免方法**：
- 关注现金流质量（经营现金流/净利润）
- 对比同行业公司，看是否异常
- 关注审计意见和会计师事务所更换情况

### 陷阱3：阈值设定太死板

ROE 15%是硬门槛吗？有些行业天生ROE就不高，比如公用事业、银行业。

**避免方法**：
- 可以考虑分行业设定不同阈值
- 或者放宽到ROE 12%，但要求其他指标特别优秀

---

## 八、优秀工程师的思考方式

从这个项目里，我们可以学到什么工程思维？

### 1. 分层设计（Separation of Concerns）

每个模块只做一件事。数据获取不管计算，规则检查不管输出。这样：
- 容易维护——改一处不会影响其他
- 容易测试——每层可以独立测试
- 容易扩展——加新功能只需加新模块

### 2. 防御式编程（Defensive Programming）

假设外部输入都是不可靠的：
- 网络可能中断 → 加重试机制
- 数据可能缺失 → 做空值检查
- 字段名可能变化 → 做多名称映射

程序要像骆驼一样"耐操"，而不是像玻璃一样一碰就碎。

### 3. 配置化（Configuration over Code）

阈值放在配置文件里，而不是硬编码在逻辑中。这样：
- 非程序员也能调参
- 不同策略可以快速切换
- 避免"魔法数字"（代码里突然冒出个0.15，没人知道是什么意思）

### 4. 缓存策略（Caching Strategy）

不是简单地"有就用，没有就重新获取"。要考虑：
- 缓存有效期——财报数据多久更新一次？
- 缓存失效策略——什么时候强制刷新？
- 存储格式——JSON还是SQLite？

### 5. 文档即代码（Docs as Code）

每个模块、每个函数都有文档字符串（docstring），说明：
- 这个函数做什么
- 参数是什么类型
- 返回值是什么
- 可能抛出什么异常

这样别人看代码时，不需要猜。

---

## 九、下一步可以做什么？

当前版本是**MVP（最小可行产品）**，已经可以用了。但可以继续扩展：

### 功能扩展

1. **历史趋势分析**
   - 不只分析最新一期，而是看过去5年的趋势
   - ROE是上升还是下降？毛利率是否稳定？

2. **同行业对比**
   - 茅台和五粮液对比，谁的指标更好？
   - 需要引入行业分类数据

3. **估值指标**
   - 加入PE、PB、PEG等估值指标
   - 好公司也要好价格

4. **可视化图表**
   - ROE趋势图、现金流结构图
   - 用matplotlib或plotly生成

### 技术优化

1. **异步获取**
   - 批量分析时，同时获取多只股票的财报
   - 大幅提高效率

2. **数据库存储**
   - 用SQLite或PostgreSQL存储历史数据
   - 支持更复杂的查询和分析

3. **Web界面**
   - 用Streamlit或Flask做个网页版
   - 输入股票代码，点按钮就出结果

---

## 十、总结

这个项目的价值不在于"它能替你投资决策"，而在于**它能帮你节省时间**。

投资就像淘金。几千只股票是沙子，值得研究的金子只有几十颗。财报筛选器就像一台"筛沙机"，帮你快速筛掉99%的沙子，只留下1%可能含金子的样本。

然后，用你的大脑去深入研究那1%——这才是人该做的事。

> **记住唐朝的话：财报分析的核心是"排除"而非"选择"。**

---

**项目文件结构**：
```
financial_screener/
├── __init__.py           # 包初始化
├── config.py             # 配置和阈值
├── cache_manager.py      # 数据缓存
├── data_fetcher.py       # 数据获取
├── indicators.py         # 指标计算
├── rules.py              # 筛选规则
├── screener.py           # 主筛选器
└── report_generator.py   # 报告生成

data/cache/               # 缓存目录
FOR[wanghai1997].md       # 本说明文档
test_screener.py          # 测试脚本
```

**最后的话**：代码只是工具，投资才是目的。愿这个项目能帮你在投资路上少走弯路。
