# 本地数据库使用说明

## 概述

现在系统支持**本地数据库存储**，可以：
- 一次下载3年历史数据永久保存
- 每日收盘后增量更新（只下载当天新数据）
- 分析时从本地读取，毫秒级响应
- 完全离线可用，不受网络限制

---

## 快速开始

### 步骤1：下载数据

```python
from data_manager import DataManager

# 初始化数据管理器
dm = DataManager()

# 方法A：下载单只股票（测试用）
dm.download_stock("600519")  # 下载茅台的所有数据

# 方法B：批量下载多只股票
dm.full_download(
    symbols=["600519", "000858", "600276", "300750"],  # 指定股票
    max_stocks=10  # 最多下载10只（用于测试）
)

# 方法C：下载所有A股（需要数小时，慎用）
# dm.full_download()  # 不指定symbols则下载全部5000+只
```

### 步骤2：每日更新数据

```python
from data_manager import DataManager

dm = DataManager()

# 检查是否需要更新
# 如果今天已经更新过，会自动跳过
updated = dm.daily_update()
print(f"更新了 {updated} 只股票")
```

### 步骤3：使用本地数据进行分析

**财报筛选器：**
```python
from financial_screener import FinancialScreener

# 使用本地数据库模式
screener = FinancialScreener(use_cache=True, use_local_db=True)
result = screener.screen_single("600519")
```

**投资大师系统：**
```python
from investment_master import MasterAnalyzer

# 自动使用本地数据库（默认）
analyzer = MasterAnalyzer()  # 等价于 MasterAnalyzer(use_local_db=True)
result = analyzer.analyze("600519")
```

---

## 数据存储结构

```
data/
└── stock_data.db          # SQLite数据库文件
    ├── stocks             # 股票基本信息
    ├── daily_prices       # 日线数据（3年+）
    ├── weekly_prices      # 周线数据（3年+）
    ├── min60_prices       # 60分钟数据（30天）
    ├── min15_prices       # 15分钟数据（15天）
    ├── financial_abstract # 财务摘要
    ├── balance_sheet      # 资产负债表
    ├── income_statement   # 利润表
    ├── cash_flow          # 现金流量表
    └── update_log         # 更新日志
```

---

## 完整工作流程

### 首次使用（初始化）

```python
# 1. 初始化并下载你关注的股票
from data_manager import DataManager

dm = DataManager()

# 下载你关注的股票池（示例：20只）
my_watchlist = [
    "600519", "000858", "600809",  # 白酒
    "000333", "000651", "600690",  # 家电
    "600276", "300760", "600436",  # 医药
    "002415", "300124", "603501",  # 科技
    "300750", "601012", "002594",  # 新能源
    "600036", "601318",           # 金融
    "601899", "600900",           # 资源/电力
]

# 下载所有数据（约需20-30分钟）
dm.full_download(symbols=my_watchlist)
```

### 日常操作（收盘后）

```python
# 2. 每天收盘后更新数据（只需1-2分钟）
dm.daily_update()

# 3. 使用整合选股系统分析
from integrated_stock_picker import IntegratedStockPicker

picker = IntegratedStockPicker()
results = picker.pick_stocks(my_watchlist, min_confidence=65)

# 查看双重确认的股票
for stock in results['double_confirmed']:
    print(f"{stock['name']}: 置信度{stock['technical_confidence']:.1f}%")
```

---

## 常见问题

### Q1：数据库文件在哪里？

默认位置：`data/stock_data.db`

你可以指定其他路径：
```python
dm = DataManager(db_path="D:/我的数据/stocks.db")
```

### Q2：数据会过期吗？

- **日线/周线**：不会过期，但需要每天更新以获取最新数据
- **分钟线**：有有效期（只存最近15-30天）
- **财务数据**：季报/年报发布后需要重新下载

### Q3：如何重新下载某只股票？

```python
# 重新下载会覆盖旧数据
dm.download_stock("600519")
```

### Q4：如果本地没有数据会怎样？

系统会自动回退到AKShare实时模式，但会打印警告信息。

### Q5：数据下载失败了怎么办？

1. 检查网络连接
2. 等待几分钟后重试（AKShare可能有频率限制）
3. 如果还是失败，可以第二天再试

---

## 数据量参考

| 数据类型 | 存储空间（单只） | 100只合计 |
|---------|----------------|----------|
| 日线3年 | ~500KB | ~50MB |
| 周线3年 | ~100KB | ~10MB |
| 60分钟30天 | ~200KB | ~20MB |
| 15分钟15天 | ~150KB | ~15MB |
| 财务数据 | ~50KB | ~5MB |
| **合计** | **~1MB** | **~100MB** |

---

## 下一步

1. 先运行 `python download_data.py` 下载你关注的股票
2. 然后运行 `python test_local_db.py` 验证分析功能
3. 以后每天收盘后运行 `python daily_update.py` 更新数据
